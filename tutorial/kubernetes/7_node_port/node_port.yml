apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx-app
    type: back-end
  ports:
    - port: 80 # Service의 포트
      targetPort: 80 # Container의 포트
      nodePort: 30080 # 외부 Node의 포트